---
"@chbrain/khai-plays-kleist": patch
---

Sync @chbrain/khai-tests to ^0.1.19 and let the version script heal registry.json (`changeset version && khai-tests registry build`); cast each plot's company inline so the casting gate holds.
