---
---

governance: roll the khai-arch devDependency to 0.1.16 in the lockfile, so the
house's conformance CI enforces the canon's now-required play `description`.
Dev-only; the published package is unchanged. No release.
