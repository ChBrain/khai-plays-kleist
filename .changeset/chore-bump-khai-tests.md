---
"@chbrain/khai-plays-kleist": patch
---

deps: bump `@chbrain/khai-tests` to 0.1.20 (lockfile), which adds the `titleLeakAudit` reviewer-assist. Conformance stays green; the assist surfaces source-language-in-`title:` candidates as audit notes only.
